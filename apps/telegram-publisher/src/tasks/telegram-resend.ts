import { Task, TaskId } from "@freestuffbot/rabbit-hole"


export default async function handleAppsResend(task: Task<TaskId.TELEGRAM_RESEND>): Promise<boolean> {
  // TODO handle task, return if successful

  return true
}
