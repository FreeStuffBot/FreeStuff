{
  "name": "@freestuffbot/typings",
  "version": "1.3.5",
  "description": "FreeStuff Typings",
  "main": "index.js",
  "types": "types/index.d.ts",
  "scripts": {
    "lint": "eslint --ext .ts --ignore-path .gitignore types/*.d.ts",
    "test": "echo \"Error: no tests specified\" && exit 0",
    "prepub": "npm run lint && npm run build && npm run test && npm pub"
  },
  "publishConfig": {
    "@freestuffbot:registry": "https://npm.pkg.github.com"
  },
  "author": "FreeStuff Team",
  "devDependencies": {
    "@types/node": "^16.3.2",
    "config": "*",
    "tsconfig": "*",
    "eslint": "^7.30.0",
    "reflect-metadata": "^0.1.13",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "@types/bson": "^4.2.0",
    "cordo": "^1.7.0",
    "freestuff": "^1.8.0"
  }
}
